---
title: Google Cloud Deployment
description: "Deploying Winter Backend Cardano on Google Cloud Platform"
order: 1
---

# Google Cloud Deployment

Within zenGate, we are using Google Cloud as our hosting infrastructure for most of our projects, so below are some good steps to follow for using this provider.

- Use Google Cloud Run for deploying the containerized application, make sure to provide enough memory to the instance, otherwise the project will fail to startup. More than 500 MB will be needed.
- Use Google Cloud SQL for hosting the required Postgres database instance. The queue runs inside Postgres with pg-boss. There is no Redis or Memorystore to provision.
- The repository uses a GitHub workflow with a manual workflow_dispatch trigger to deploy. There is no automatic deploy on push or merge.

> Note: This page predates the current deployment workflow and queue design. Treat the Redis and trigger details as outdated. The statements above reflect the current system. A full rewrite is planned.

## Common Google Cloud Deployment Errors

- The deployment instance does not have enough memory assigned to it.
- The deployment instance does not have correct environment variables assigned to it, so certain services will not be initialized properly or fail to communicate with other services.

## Other Hosting Providers

The setup process is similar for other providers. Ensure access to Postgres. No Redis is required.
